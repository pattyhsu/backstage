.DS_Store
.obsidian/

# Local-only secrets — never commit
*.env
.env*
secrets/

# Scratch/preview artifacts
_tmp_*

# Local dev tooling & backups
.claude/
.backups/
# Local render harnesses — these embed the SERVICE key. Never commit.
_*-local.html
HANDOFF.md
make-pd-local.sh
